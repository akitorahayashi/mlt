name: CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  run-static-checks:
    uses: ./.github/workflows/run-static-checks.yml

  run-tests:
    uses: ./.github/workflows/run-tests.yml
